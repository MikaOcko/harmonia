            <nav class="navbar navbar-expand-lg bg-body-tertiary">
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ path('app_home') }}">Simplon records</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="{{ path('app_style') }}">Genres</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="{{ path('app_artist') }}">Artistes</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="{{ path('app_track') }}">Morceaux</a>
                            </li>

                            {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="{{ path('app_profile')}}">Profile</a>
                            </li>
                            {% endif %}
                        </ul>

                        {# Styles dropwdown #}
                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Genres
                            </button>
                            <ul class="dropdown-menu">
                                {%set styles = get_styles() %}
                                {% for style in styles %}
                                    <li><a class="dropdown-item" href="{{ path('app_style_name', {'name' : style.name})}}">{{ style.name }}</a></li>
                                {% endfor %}
                            </ul>
                        </div>

                        {# Je suis connecté #}
                        {% if app.user %}
                            <a href="{{ path('app_logout') }}" class="btn btn-danger">Se déconnecter</a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="btn btn-success">Se connecter</a>
                        {% endif %}
                    </div>
                </div>
            </nav>
            